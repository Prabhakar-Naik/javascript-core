var list = ["mango", "kiwi", "orange", "papaya"];

console.log(list);

var banana = "banana";
list.push(banana); // to add value at last in an array

console.log(list);

list.pop(); // remove last element in an array
console.log(list);

console.log(list[0]);

list[0] = "strawberry"; // update value at 0 index in an array
console.log(list);

list.shift(); // remove first elelment in an array
console.log(list);
